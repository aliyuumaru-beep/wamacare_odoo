<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Safeguarding Data Access group — can see all beneficiary + case records -->
    <record id="group_safeguarding_user" model="res.groups">
        <field name="name">Safeguarding User</field>
        <field name="category_id" ref="base.module_category_hidden"/>
        <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
    </record>
</odoo>
